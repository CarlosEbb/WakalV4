---
import ButtonBack from "./ButtonBack.astro";

interface Props {
	isButtonBack?: boolean;
}

const { isButtonBack = true} = Astro.props;

---

<div class="relative">
    {isButtonBack && <ButtonBack/>}
    <div class="bg-white px-6 -mt-16 mb-8 mx-6 rounded-lg py-8">
        <slot/>
    </div>
</div>